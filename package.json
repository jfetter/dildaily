{
  "name": "dildaily",
  "private": true,
  "version": "0.0.0",
  "devDependencies": {
    "bower": "^1.3.1",
    "http-server": "^0.6.1",
  },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install"
  }
}
